# TASKS — 예정 작업

최종 갱신: 2026-08-31 (CONTENT-4 배치)

우선순위: **A = 총회 직후 필수** / **B = 안정화 후** / **C = 여유 있을 때**

---

## A. 2026-08-29 창립총회 직후 (확정 사항 반영)

- [ ] **임원 명단 게시** — `people.html`
      현재 조직도 박스는 "대표 김태헌 외 임원" / "총회 선출"로 자리만 잡아둔 상태.
      하단 주석 "※ 임원 명단은 2026. 8. 29. 창립총회 선출 결과에 따라 게시됩니다." 도 함께 정리.
- [ ] **확정 정관으로 교체** — `jeongwan.html`
      현재는 "창립총회 상정본(안)". 의결본으로 전문 교체하고 상단 메타
      "2026. 8. 29. 창립총회 상정본 · 총회 의결로 확정됩니다" 문구 갱신.
      `<title>`·자료실 링크 문구의 "(안)" 표기도 같이 정리.
      `resource.html` 하단 주석 "※ 정관과 사업계획은 … 확정본으로 갱신 게시됩니다." 도 함께.
- [ ] **총회 결과 공지 게시** — `notice.html`
      `#post3` 신설. 목록에도 1행 추가. `.article`에 `scroll-margin-top`이 이미
      적용되어 있으므로 같은 클래스를 쓰면 앵커 위치 문제 없음.
      index.html 최근 소식 목록에도 반영(두 곳의 날짜가 어긋나지 않도록 주의 — 이번 QA에서 실제로 어긋나 있었음).
- [ ] **팝업 제거** — `index.html`
      2026-08-29 18:00 KST 이후 자동으로 안 뜨도록 되어 있어 급하진 않음.
      다만 마크업·스크립트가 남아 있으므로 총회 후 정리.
      대상: `.popup-veil` 블록, 그 직후 `<script>` 블록, `<style>`의 `.popup*` 규칙.
- [ ] **확정 사업계획 게시**
      QA-1에서 `plan.html`(세부사업계획서(안))은 고아 파일이라 삭제함.
      의결본을 올릴 때는 새로 만들고 `resource.html` 목록에 링크 추가.

## A. 명의 이전 (조합 설립인가 후)

- [ ] **도메인 `sinan-olive.org` 명의 이전** — 개인 → 조합
- [ ] **GitHub 저장소 이전** — `paulsong7777/sinan-olive-web` → 조합 계정/Organization
      이전 시 GitHub Pages 재설정 + CNAME·DNS 재확인 필요. 잠시 접속 끊길 수 있으니
      공고 기간을 피해서 진행할 것.

## B. 안정화

- [ ] **외부 링크 실제 동작 확인** (QA 환경에서 미검증)
      coop.go.kr / shinan.go.kr / map.kakao.com 길찾기 / contact.html 구글맵 iframe.
      실배포 URL에서 직접 클릭 확인.
- [ ] **웹폰트 로딩 확인** (QA 환경에서 미검증)
      Gowun Batang(Google Fonts), Pretendard(jsDelivr)가 실제 적용되는지 육안 확인.
      jsDelivr는 국내 일부 망에서 느릴 수 있음 → 문제 시 폰트 로컬 호스팅 검토.
- [ ] **이미지 최적화**
      4장 합계 약 2.5MB, 원본 해상도 그대로 서빙 중. farm_aerial.jpg 단독 1.1MB.
      리사이즈 + WebP 검토. 섬 지역 모바일 접속 고려하면 체감 차이 큼.
- [ ] **공통 CSS 중복 해소 검토**
      동일한 `<style>` 블록이 10개 파일에 복제되어 있음. 공통 .css 파일로 분리하면
      유지보수가 쉬워지지만, 파일이 분리되는 만큼 배포 사고 위험도 생김.
      총회 등 중요 일정이 없는 시기에 진행할 것.

## C. 접근성 · SEO (기록만, 이번 배치 대상 아님)

- [ ] 팝업 접근성: `aria-modal="true"`, 포커스 트랩, ESC 닫기, 오버레이 클릭 닫기
- [ ] 햄버거 버튼에 `aria-expanded` 토글 추가 (현재 없음)
- [ ] meta description이 10개 페이지 전부 동일 → 페이지별 문구로 분리
- [ ] index.html 최근 소식 링크를 `notice.html#post1` / `#post2`로 세분화
      (현재 둘 다 `notice.html` 최상단으로 이동)
- [ ] OG 이미지가 전 페이지 `hero.jpg` 공통 → 페이지별 분리 검토
- [ ] 구조화 데이터(JSON-LD Organization) 추가 검토

---

## 이번 배치에서 발견했으나 **의도적으로 손대지 않은 것**

디자인·레이아웃·컬러·문구 톤 변경은 이번 범위에서 제외(사고 이력). 기록만 남김.

- `contact.html`이 다른 페이지 대비 내용이 얇음. 문의 폼이 제거된 흔적으로 보이며
  `<style>`에 `.form` 규칙이 남아 있으나 사용하는 마크업이 없음(죽은 CSS).
  → 폼 기능 부활은 이번 범위 밖. 판단 후 정리 필요.
- `story.html`에 "※ 원문 기사와 도서 안내 링크는 게재 협의 후 연결됩니다." 안내가 있고
  story1/story2에는 `<!-- [원문 게재 자리] -->` 주석이 남아 있음. 저작권 협의 진행 상황 확인 필요.
- 주소 표기가 전 페이지 "전남광주통합특별시 신안군 신의면 안산길 100-15 (상태서리 268)"로
  **일관됨**. 다만 행정구역 명칭이 실제 확정 명칭과 맞는지는 대외 문서와 한 번 대조 권장.
  (일관성 문제는 아니므로 이번 배치에서 변경하지 않음)
- `notice.html` `#post2` 앵커로 이동 시 페이지 최하단이라 스크롤이 끝까지 가지 못함.
  내용은 정상적으로 다 보이므로 결함 아님.

---

## 배치 이력

### CONTENT-4 (2026-08-31) — 창립총회 보도자료 게시 + 총회 이후 표현 정리
- **press1.html 신규**: 창립총회 보도자료 전문 + 설립취지문 + 현장 사진 2장.
  notice.html을 구조 템플릿으로 사용, `<style>` 블록 동일. 새 CSS·색상·폰트 0건.
  사진 `images/founding_meeting_1.jpg`(1130x851) / `_2.jpg`(908x706) 함께 커밋.
- **notice.html**: 상단 목록에 결과 공지 1행 추가, `#post3` 요약 article 신설
  (전문은 press1.html로 연결, 중복 게재 안 함). `#post1`·`#post2`는 기록 보존.
- **index.html**: 히어로 CTA `창립총회 안내 보기`(notice) →
  `창립총회 결과 보기`(press1). 최근 소식 맨 위에 결과 공지 추가.
- **og:description 10개 페이지 전수 교체**: 지난 총회 예고
  (`창립총회 2026. 8. 29.`)가 SNS 미리보기에 계속 노출되던 문제 해소.
- **resource.html**: `확정되며 … 게시됩니다` 미래형 → 총회 이후 시점 표현으로 정정.
- **sitemap.xml**: press1.html 추가(priority 0.8), lastmod 갱신.

### CONTENT-3 (2026-08-26) — people.html
- 감사 2인 직함 정정: 김맹수 감사 → **업무감사**, 강황배 감사 → **재무감사**
- 조직도 계층 재구성: 총회 → 대의원회 → 이사회(중앙 수직축) → 사무국,
  감사는 이사회와 같은 높이에서 우측 분기 + CSS 수평 연결선.
  820px 이하는 1열(총회→대의원회→이사회→감사→사무국).
  연결선 전부 CSS 가상요소/보더, 이미지·SVG 0건, 신규 색상·폰트 0건.

### CONTENT-2 (2026-08-26)
- story2: 기사 전문 게재 취소 → 경향신문 원문 링크로 대체
- story: 목록·안내 문구 정리 (아래 C 섹션의 "게재 협의 후 연결" 관련 항목 해소됨)
- people: 임원진을 사업계획서 임원 현황 기준으로 교체(사무국 표기 제외)
- about: 연혁 3건 보완 후 시간순 재배치

### CONTENT-1 (2026-08-26)
- about: 목표·문제분석·SWOT·연혁 4개 섹션 추가
- people: 조직도 교체 + 임원진 추가, 축약 연혁 제거
- story1: 협회 자체 명상 원고 전문 게재

### QA-1 (2026-08-25)
- 링크 전수·모바일·스티키 푸터·팝업·콘솔 점검, plan.html 삭제,
  공지 앵커 스크롤 보정, 팝업 localStorage/만료 구현, sitemap·robots 추가

---

## 이번 배치(CONTENT-3)에서 기록만 하고 손대지 않은 것

- **www.sinan-olive.org 인증서 미발급** — GitHub DNS 체크 잔여. 별건 추적 중.
  코드로 해결 불가 영역이므로 이번 배치에서 제외.
- **총회(2026-08-29) 후 반영분** — 선출 임원 확정 명단, 확정 정관 교체,
  총회 결과 공지, 사업자등록번호 표기, 연혁 추가. 총회 전에는 손대지 않음.
- **조직도 사무국 인원 표기** — 클라이언트가 미표기로 지시함. 변경 금지.
- **다른 페이지(index/about/notice 등)의 유사 표현 정리** — 이번 범위 아님.

### CONTENT-3 작업 중 확인된 참고사항
- 조직도는 공용 스타일시트의 `.org` / `.org .box` 클래스를 쓰지 않는다.
  CONTENT-1에서 인라인 스타일 마크업으로 교체되었고, 이번 배치에서
  페이지 로컬 `.orgchart` 클래스로 전환했다. 공용 `.org` 규칙은 여전히
  어느 페이지에서도 쓰이지 않는 죽은 CSS 상태.
- 조직도 박스에는 `<b>` 제목 / `<span>` 부제 구조가 없다. 평문 라벨 1줄뿐이며
  최상단 라벨은 "총회"가 아니라 "조합원 총회"다.

---

## CONTENT-4에서 기록만 하고 손대지 않은 것

- **확정 정관 교체 및 "(안)·상정본" 표기 제거** — 확정본 수령 후 별도 배치.
  현재 `jeongwan.html`, `resource.html`, `about.html`에 "(안)" 표기가 남아 있음.
- **사업자등록번호 → 푸터·Contacts 반영** — 설립인가 후.
- **연혁에 "설립인가 신청/취득" 추가** — 인가 후.
- **CONTENT-5로 분리**: meta description 10개 페이지 개별화 /
  canonical 태그 추가 / 이미지 최적화(2.45MB → 1.32MB 목표).
- **www.sinan-olive.org 인증서 미발급** — GitHub DNS 체크 잔여, 별건 추적 중.
- **팝업 코드** — 손대지 않음. 만료 로직(2026-08-29 18:00 KST 이후 자동 미노출)이
  정상 작동 중이며 다음 행사에 재사용할 검증된 템플릿이므로 보존.
